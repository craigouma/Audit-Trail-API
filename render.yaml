services:
  - type: web
    name: audit-trail-api
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 10000 